apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: rhdp-scheduler

commonLabels:
  app.kubernetes.io/name: rhdp-scheduler
  app.kubernetes.io/part-of: rhdp-scheduler

resources:
  - imagestream.yaml
  - buildconfig.yaml
  - serviceaccount.yaml
  - clusterrole.yaml
  - clusterrolebinding.yaml
  - cluster-targets-rbac.yaml
  - configmap.yaml
  - secret.yaml
  - pvc.yaml
  - deployment.yaml
  - service.yaml
  - route.yaml
  - networkpolicy.yaml
