apiVersion: v1
kind: ConfigMap
metadata:
  name: rhdp-scheduler-config
data:
  LOG_FORMAT: "json"
  LOG_LEVEL: "INFO"
  RHDP_LOG_DIR: "/app/logs"
  AGNOSTICV_REPO_URL: "git@github.com:rhpds/agnosticv.git"
  AGNOSTICV_CACHE_DIR: "/tmp/agnosticv-cache"
  AGNOSTICV_REFRESH_TTL_SECONDS: "900"
